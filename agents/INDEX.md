# Agents

One-line summaries. Update when agents are added, removed, split, or renamed.

- **Toolmaker** — Classifies build intent (tool / skill / existing-agent / split / new-agent / edit-target), writes intent docs, and builds agents and skills when the verdict is unambiguous. Pauses for tools / hooks / commands and for boundary cases. Maintains this index.
